import React from 'react';
import styles from '../styles/AdminNavbar.module.css';

const MyNavbar = () => {
    return (
        <nav className={styles.navbar}>
            <div className={styles.navbarTitle}>TUNE VAULT</div>
        </nav>
    );
};

export default MyNavbar;
